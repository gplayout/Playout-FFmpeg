<vbase:BaseWindowView x:Class="Playout.UI.Views.MediaOverlayPreviewView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vbase="clr-namespace:Playout.UI.Base"
        xmlns:controls="clr-namespace:Playout.UI.Controls"
        xmlns:metro="http://schemas.codeplex.com/elysium"
        xmlns:params="http://schemas.codeplex.com/elysium/params"
        xmlns:extra="http://schemas.extra.com/ui"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        ResizeMode="NoResize" Closing="BaseWindowView_Closing"
        Title="Media Overlay Preview" Height="650" Width="750">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" HorizontalAlignment="Left">
            <controls:PlayerControlView x:Name="playerControl"/>
        </Border>
        <Border Grid.Row="1" Margin="3"
                Style="{StaticResource BorderStyle}">
            <controls:Player Grid.Column="0"  x:Name="videoPlayerUC" Height="480" Width="600"/>
        </Border>
    </Grid>
</vbase:BaseWindowView>
